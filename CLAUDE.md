Details and overview of the project are in @./README.md .

If you are editing note parsing (in src/note_analyser.rs) you can also use examples/dump_notes.rs to check the current behaviour of the note parser on sample input.

Run `cargo clippy` and `cargo fmt` after every code change in this repo. Do not accept pre-existing clippy errors: it should be clean at all times.